```
Create a reusable prompt file named `.github/prompts/refresh-docs.prompt.md`.

 This prompt should help our team update `ARCHITECTURE.md` and
`.github/copilot-instructions.md` after meaningful code changes.

 Please make the prompt:
 - review the current workspace for recent code changes
 - update those docs only where the codebase has actually changed
 - keep the updates concise, accurate, and aligned to the current repository
 - avoid rewriting unrelated sections
 - make the prompt reusable after future code changes

 Generate the full prompt file with frontmatter and prompt content.
```
