Updated memory file [](file:///c%3A/Users/rmathis/AppData/Roaming/Code/User/workspaceStorage/5169a291372cdee6bffaf93d6a8f5292/GitHub.copilot-chat/memory-tool/memories/NmE5ZjAzMmQtMjVhNS00NmVmLWI0OTUtYmE1M2MzNDYwNmEz/plan.md)

Perfect. Here's your finalized plan:

---

## Plan: Add 200-Char Tagline to Character Detail Header

**Scope:** Add new `Tagline` property to Character model → display directly under name on detail page only → 200-char limit with validation.

**Files to Update:**

- Backend/Models/Character.cs — Add `Tagline` property
- Backend/Data/FanHubContext.cs — Seed tagline data
- Backend/Migrations/ — Add column (EF migration)
- Frontend/Components/CharacterHeader.razor — Display under `<h1>`
- Frontend/wwwroot/css/character-detail.css — Optional light styling
- Backend.Tests/Models/CharacterModelTests.cs — Validation tests

---

## Implementation Order

**Phase 1: Backend Model**

1. Add `[Required]` `[StringLength(200)]` `public string Tagline { get; set; }` to Character.cs
2. Run `dotnet ef migrations add AddCharacterTagline`
3. Run `dotnet ef database update`
4. Seed taglines in FanHubContext.cs (e.g., _"Complex anti-hero with a troubled past"_)

**Phase 2: Backend Tests** 5. Add unit tests:

- Tagline required (not null)
- Tagline max 200 chars enforced
- Tagline empty string rejected

6. Verify API integration test includes tagline in `/api/characters/{id}` response

**Phase 3: Frontend Display** _(parallel with Phase 2)_ 7. Update CharacterHeader.razor:

```html
<h1>@Character.Name</h1>
@if (!string.IsNullOrEmpty(Character.Tagline)) {
<p class="character-tagline">@Character.Tagline</p>
}
```

8. Add CSS (optional): `.character-tagline { font-size: 1.1em; font-style: italic; margin-top: 0.5em; }`

**Phase 4: Verify** 9. Run app → navigate to character detail page → confirm tagline appears under name 10. Test via Swagger or REST Client that tagline is in API response

---

## Testing Checklist

- [ ] Character model validates [Required]
- [ ] Character model enforces [StringLength(200)]
- [ ] Empty tagline rejected
- [ ] Migration applied successfully
- [ ] GET /api/characters/{id} returns tagline
- [ ] CharacterHeader displays tagline (if present)
- [ ] Tagline null-safe (no crash if missing)

---

Ready to implement? Or would you like me to walk through any specific step first?
