```
Use the make-skill-template skill to create a new agent skill named
 character-change-test-workflow in .github/skills/.

 This skill should help our team review a character-detail change and decide
 what tests should be added or updated before we open a PR.

 The skill should be useful when:
 - a new character field is added
 - rendering rules change
 - fallback or empty-state behavior changes
 - partial or missing data needs coverage
 - we want to check for likely regressions

 Please scaffold the skill so it includes:
 - a strong SKILL.md with keyword-rich frontmatter
 - a reusable review template for deciding what tests should change
 - an edge-cases checklist
 - example patterns showing common character-detail test updates

 The SKILL.md description frontmatter must include trigger keywords that cover
 both direct test-authoring scenarios AND code review scenarios, so the skill
 is auto-discovered whenever a character-detail code review prompt runs. Include
 phrases such as:
 - "character detail code review"
 - "character detail review"
 - "PR review for character changes"
 - "reviewing character detail feature implementations"
 - "verify character rendering correctness"
 - "character PR review"
 - "character detail page review"

 This ensures the skill is picked up automatically — without any hard-coded
 file reference — by any prompt whose content uses this language.

 The workflow should help Copilot:
 1. understand what changed
 2. identify which tests should be updated or added
 3. highlight important edge cases
 4. help generate or revise the highest-priority tests
```
