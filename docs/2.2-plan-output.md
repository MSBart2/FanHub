Got it. Let me update the plan with your decisions:

**Q1: Permissive** ✅ — No validation on CharacterType (keep simple)
**Q2: Types as appropriate** ✅ — Hero, Villain, Antihero, Comic Relief, etc. (flexible)
**Q3: Multiple types per character** ✅ — Support multiple badges per character

---

## Plan: Add Status & Character Type Badges (Multi-Type Support)

**Scope:** Add `CharacterType` field supporting multiple types (comma-separated) + display as visual badges on detail page header.

**Files to Update:**

- Backend/Models/Character.cs — Add `CharacterType` property (string, supports "Hero,Antihero")
- Backend/Data/FanHubContext.cs — Seed CharacterType with multiple values where appropriate
- Backend/Migrations/ — Add CharacterType column via `dotnet ef migrations`
- Frontend/Components/CharacterHeader.razor — Display both StatusBadge and CharacterTypeBadge
- Frontend/Components/StatusBadge.razor — **New** — Single status badge
- Frontend/Components/CharacterTypeBadge.razor — **New** — Multiple type badges (comma-separated parsing)
- Frontend/wwwroot/css/badges.css — Badge styling
- Backend.Tests/Models/CharacterModelTests.cs — Optional basic tests

---

## Implementation Order

**Phase 1: Backend Model & Data**

1. Add `public string CharacterType { get; set; }` to Character.cs (no attributes, permissive)
2. Run `dotnet ef migrations add AddCharacterType`
3. Run `dotnet ef database update`
4. Seed CharacterType in FanHubContext.cs with flexible values:
   - Walter White → `"Villain,Antihero"`
   - Hank Schrader → `"Hero"`
   - Jesse Pinkman → `"Antihero,Comic Relief"`
   - Skyler White → `"Hero"` (etc.)

**Phase 2: Frontend Components** _(parallel with Phase 1)_ 5. Create `StatusBadge.razor`:

```html
@if (!string.IsNullOrEmpty(Status)) {
<span class="badge badge-status badge-@Status.ToLower()">@Status</span>
} @code { [Parameter] public string Status { get; set; } }
```

6. Create `CharacterTypeBadge.razor` — Parse comma-separated types:

   ```html
   @if (!string.IsNullOrEmpty(CharacterType)) { @foreach (var type in types) {
   <span class="badge badge-type badge-@type.ToLower()">@type</span>
   } } @code { [Parameter] public string CharacterType { get; set; } private
   string[] types = Array.Empty<string
     >(); protected override void OnParametersSet() { types =
     string.IsNullOrEmpty(CharacterType) ? Array.Empty<string
       >() : CharacterType.Split(',').Select(t => t.Trim()).ToArray(); }
       }</string
     ></string
   >
   ```

7. Update CharacterHeader.razor:
   ```html
   <h1>@Character.Name</h1>
   <div class="character-badges">
     <StatusBadge Status="@Character.Status" />
     <CharacterTypeBadge CharacterType="@Character.CharacterType" />
   </div>
   ```

**Phase 3: Styling** 8. Add CSS for badges (reuse `.episode-badge` precedent):

```css
.character-badges {
  margin-top: 0.5em;
  margin-bottom: 1em;
}

.badge {
  display: inline-block;
  padding: 0.4em 0.8em;
  border-radius: 4px;
  font-size: 0.9em;
  font-weight: 500;
  margin-right: 0.5em;
  margin-bottom: 0.3em;
}

/* Status badges */
.badge-status.badge-alive {
  background: #4caf50;
  color: white;
}
.badge-status.badge-deceased {
  background: #757575;
  color: white;
}

/* Character type badges */
.badge-type.badge-hero {
  background: #2196f3;
  color: white;
}
.badge-type.badge-villain {
  background: #f44336;
  color: white;
}
.badge-type.badge-antihero {
  background: #ff9800;
  color: white;
}
.badge-type.badge-comic-relief {
  background: #9c27b0;
  color: white;
}
```

**Phase 4: Verify** 9. Run app → check character detail page → Status + CharacterType badges display under name 10. Test multiple types display as separate badges (e.g., Walter White shows both "Villain" and "Antihero") 11. Test API response includes CharacterType field

---

## Test Checklist

- [ ] CharacterType property added to model
- [ ] Migration applied successfully
- [ ] Seed data includes multi-type values (comma-separated)
- [ ] GET /api/characters/{id} returns CharacterType field
- [ ] StatusBadge renders correctly (null-safe)
- [ ] CharacterTypeBadge parses comma-separated types correctly
- [ ] Multiple badges display separately with proper spacing
- [ ] Single type displays as single badge
- [ ] Colors match design (Status green/gray, Type blue/red/orange/purple)

---

## Key Implementation Notes

- **Multiple types format:** `"Hero,Antihero"` (handled by split + trim in component)
- **Null-safe:** Both badge components check for empty/null before rendering
- **Permissive validation:** No backend validation—just a string field (you decide what goes in seed data)
- **CSS flexibility:** Easy to add new badge colors as character types expand

Ready to implement, or want to refine anything first?
