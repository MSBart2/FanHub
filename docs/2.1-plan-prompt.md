```
/plan Help me plan a small enhancement to the character detail page.

 I want to add a short tagline or summary under the character name.
 Please use the existing FanHub patterns and give me:
 - the files I’d likely need to update
 - a good order for the work
 - any open questions I should answer first
 - tests I should include

 Keep this lightweight and don’t turn it into a bigger feature.
```

---

Read skill [openapi-endpoint-design](file:///c%3A/Users/rmathis/source/FanHub-1/.github/skills/openapi-endpoint-design/SKILL.md?vscodeLinkType%3Dskill#1-1), lines 1 to 80

Read [](file:///c%3A/Users/rmathis/source/FanHub-1/dotnet/docs/FEATURE-CHARACTER-DETAIL.md#1-1), lines 1 to 150

Read [](file:///c%3A/Users/rmathis/source/FanHub-1/dotnet/Backend/Models/Character.cs)

Read [](file:///c%3A/Users/rmathis/source/FanHub-1/dotnet/Frontend/Components/CharacterHeader.razor#1-1), lines 1 to 50

Now I have the context. Let me save my planning findings and present a comprehensive plan.

Created memory file [](file:///c%3A/Users/rmathis/AppData/Roaming/Code/User/workspaceStorage/5169a291372cdee6bffaf93d6a8f5292/GitHub.copilot-chat/memory-tool/memories/NmE5ZjAzMmQtMjVhNS00NmVmLWI0OTUtYmE1M2MzNDYwNmEz/plan.md)

## Plan: Add Tagline/Summary to Character Detail Page

**TL;DR:** Adding a short tagline beneath the character name is straightforward. Two approaches: add a new `Tagline` field to the backend (explicit control) or derive it from the bio (no backend changes). The feature spec already requires a bio, so we have the data—just need to decide on presentation.

---

## 🤔 Open Questions (Answer These First)

**Q1: What's the tagline source?**

- **Option A:** New field on Character model (`Tagline` — 100-200 char limit, stored in DB)
- **Option B:** Auto-slice first 150 chars from `Bio` (frontend-only, no DB changes)

**Q2: Where exactly should it appear?**

- Right under the character name (primary heading)?
- In the meta section (next to Actor/Show)?

**Q3: Should it display on the character list page too?**

- Or only on detail page?

---

## 📋 Files to Update (Based on Option A — New Field)

**Backend:**

- Backend/Models/Character.cs — Add `Tagline` property with validation
- Backend/Data/FanHubContext.cs — Seed data for Tagline (if using `HasData()`)
- Backend/Migrations/ — Add `Tagline` column (run `dotnet ef migrations add AddCharacterTagline`)
- Backend.Tests/Models/CharacterModelTests.cs — Add validation tests

**Frontend:**

- Frontend/Components/CharacterHeader.razor — Display tagline under `<h1>`
- Frontend/wwwroot/css/character-detail.css — Add styling for tagline (optional, reuse existing classes if possible)

**API/DTO:**

- Backend/Models/DTOs.cs — Update `CharacterDetailDTO` to include `Tagline` field (if separate DTO exists)

---

## 🛠️ Recommended Order of Work

### Phase 1: Backend Model & Data (Can run independently)

1. **Update Character model** — Add `[StringLength(150)]` + `[Required]` `Tagline` property
   - Validates tagline is 1–150 characters
   - Follows FanHub pattern from ARCHITECTURE.md

2. **Create EF Core migration** — Add `Tagline` column

   ```bash
   dotnet ef migrations add AddCharacterTagline
   dotnet ef database update
   ```

3. **Seed test data** — Update FanHubContext.cs `HasData()` to populate `Tagline` for existing characters
   - Example: _"A complex anti-hero with a troubled past"_
   - Example: _"The brilliant but unstable chemist at the heart of the show"_

### Phase 2: Backend Tests

4. **Add model validation tests** — Backend.Tests/Models/CharacterModelTests.cs
   - Test `[Required]` — Tagline cannot be null
   - Test `[StringLength(150)]` — Tagline max length enforced
   - Test empty string rejection

5. **Update API integration tests** — If Backend.Tests/Integration/CharactersControllerTests.cs exists
   - Verify `GET /api/characters/{id}` returns tagline in response

### Phase 3: Frontend Display _(Can start in parallel with Phase 2)_

6. **Update CharacterHeader.razor** — Add tagline display
   - Add new `<p class="character-tagline">` div right after `<h1>@Character.Name</h1>`
   - Add null check: `@if (!string.IsNullOrEmpty(Character.Tagline))`

7. **Add CSS styling** — Minimal (reuse existing font scales)
   - Slightly smaller font than heading, larger than body text
   - Optional: italic or subtle color to differentiate from name

### Phase 4: Verification & Testing

8. **Manual testing** — Run app and verify tagline displays on character detail page
9. **API testing** — Use REST Client or Swagger to confirm `/api/characters/{id}` includes tagline

---

## 📊 Test Checklist

| Test                                          | File                                                   | Type        |
| --------------------------------------------- | ------------------------------------------------------ | ----------- |
| Tagline has [Required] validation             | Backend.Tests/Models/CharacterModelTests.cs            | Unit        |
| Tagline has [StringLength(150)] validation    | Backend.Tests/Models/CharacterModelTests.cs            | Unit        |
| GET /api/characters/{id} returns tagline      | Backend.Tests/Integration/CharactersControllerTests.cs | Integration |
| CharacterHeader displays tagline when present | Frontend component test or manual                      | Manual      |
| Tagline null-safe (doesn't crash if null)     | Frontend component test or manual                      | Manual      |

---

## 🎯 Scope Boundaries

**Included:**

- ✅ Add Tagline field to Character model
- ✅ Display tagline on character detail page
- ✅ Basic validation (required, max length)
- ✅ Unit + integration tests

**Deliberately Excluded (Keep It Small):**

- ❌ Add tagline to character list page (separate task)
- ❌ Tagline edit/admin interface (separate feature)
- ❌ Full bio rewrite or formatting enhancements
- ❌ Character profile redesign

---

## 💡 Critical Patterns to Follow

From FanHub ARCHITECTURE.md:

1. **Model validation:** `[Required]`, `[StringLength]` on properties
2. **Async/await:** All DbContext calls must use `await` (FindAsync, ToListAsync, etc.)
3. **Null safety:** Always null-check navigation properties before accessing
4. **HTTP status codes:** Already handled by existing controller (200 for GET, 404 for not found)
5. **Blazor lifecycle:** Use `OnInitializedAsync()` for data loading (already implemented in CharacterHeader)

---

## ❓ Follow-Up Questions to Confirm Plan

Before I lock this in, please clarify:

1. **Should this be a NEW `Tagline` field** (explicit, allows different text than bio), or **derive from `Bio`** (auto-slice first 150 chars)?
2. **Placement:** Directly under name in header, or in a separate "Quick Summary" section?
3. **Is this only for detail page,** or should list page show tagline too?
4. **Max length preferences:** 100 chars? 150? 200?

Once you confirm, I can provide the detailed implementation checklist.
