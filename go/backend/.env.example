# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fanhub

# Server Configuration
PORT=8080

# JWT Configuration (BUG: Never use hardcoded secrets in production!)
JWT_SECRET=super-secret-key-do-not-use-in-production
